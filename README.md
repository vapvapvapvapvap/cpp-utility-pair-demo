# C++ Utility Library: std::pair

Библиотека `<utility>` в C++ содержит полезные инструменты, одним из которых является `std::pair`.

## Что такое std::pair?

`std::pair` — это структура, которая хранит два значения (возможно разных типов).

```cpp
std::pair<int, std::string> p = {1, "hello"};
